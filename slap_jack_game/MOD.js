messageOutput_b([opponentCards.length-1]);
messageOutput( [playerCards.length -1]);

function messageOutputA(mes){
    // if()
    message_a.innerHTML = mes;
}
function messageOutput_b(mes){
    message_b.innerHTML = mes;
}
function messageOutput(mes){
    message.innerHTML = mes;
}
